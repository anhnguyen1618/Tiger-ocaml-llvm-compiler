let
	var a := "string1"
	var b := "string1"
	var result := str_cmp(a, b)
	var c := str_concat(a, "hello world")
in
	printInt(result);
	print(c);
	if result then (print("true"); 1) else (print("false"); 0)
end