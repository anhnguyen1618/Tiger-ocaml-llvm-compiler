let
	type myint = int
	type arrtype = array of myint
	var x := 5
	var arr1:arrtype := arrtype [x] of 6
	function f (n: arrtype): int = (printInt(n[2]); 1)
in
	arr1[2] := 10; 
	printInt(arr1[4]);
	f(arr1);
	printInt(arr1[2])
end