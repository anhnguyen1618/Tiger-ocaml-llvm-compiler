let
	type rec = {name : string}
	var record := rec {name = "hello"}
	type arr = array of rec
	var list := arr [5] of record
	function f () =
		 let
			var another_rec := rec {name = "all"}
		 in
		 	list[0] := another_rec;
			print (list[0].name)
		 end
in
	f();
	printInt(5);
	print (list[0].name)
end
